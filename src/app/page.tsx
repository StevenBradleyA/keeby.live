"use client";
import KeebTypeFooter from "./_components/Footer/minimalFooter";


export default function Home() {
    // typing game be the homepage
    // redesign a more modern nav bar
    // redesign the menu system because it is very ugly
    //  combine stuff we like from other sites
    // i don't think have a left menu bar is a bad idea neccassarily idk tho maybe top is king...
    // lets make typing hide the menu and footer.
    // I want it to be clean but also unique...
    // import { getServerAuthSession } from "~/server/auth";
    // import { LatestPost } from "~/app/_components/post";

    return (
        <div className="w-full h-screen overflow-hidden">
            <div className="h-[95%] w-full flex gap-10 px-10 ">
        




            </div>

            <div className="w-full h-[5%] p-1 flex justify-center items-end">
                <KeebTypeFooter />
            </div>
        </div>
    );
}
